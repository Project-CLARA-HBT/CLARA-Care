"""Clara Scribe enterprise package (ASR seam, templates, note generation).

All modules here are additive and flag-gated; importing them opens no socket and
builds no client, so the legacy batch scribe path is unaffected.
"""
