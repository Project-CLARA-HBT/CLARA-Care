"use client";

// Promoted into the shared design system. This module re-exports the canonical
// primitives from components/ui/surface so existing imports keep working while
// benefiting from the modernized Fluent styling.
export {
  SurfaceCard,
  StatCard,
  InlineError,
  LoadingCards,
  EmptyState,
} from "@/components/ui/surface";
