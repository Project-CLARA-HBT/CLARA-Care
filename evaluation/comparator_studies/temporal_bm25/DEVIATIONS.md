# Capability boundary

This is a repository-defined deterministic baseline, not a paper reproduction.
It deliberately does not use embeddings, an LLM reranker, hidden labels,
future evidence, lifecycle outcomes, or task-specific IDs.  Any comparison
must report its fixed top-5 context budget and the absence of governance.
