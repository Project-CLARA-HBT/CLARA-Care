# Last-write-wins source mapping

The method is a repository-defined baseline, version `resource-code-lww-v1`, implemented by `commitloop_baselines.last_write_wins`. It makes no claim of reproducing an external clinical system.
