# Long-context deviations

There is no compression, learned ordering, retrieval, or governance layer. Token-budget truncation is not applied in Phase-A fixtures; any later context cap must create a new comparator version.
