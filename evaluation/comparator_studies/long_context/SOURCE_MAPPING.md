# Long-context source mapping

The method is a repository-defined baseline, version `chronological-v1`, implemented by `commitloop_baselines.long_context`. It is not attributed to an external paper and has no claimed published performance.
