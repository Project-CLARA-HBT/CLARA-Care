# Long-context method card

This frozen local baseline sorts every visible authorized event by valid time and evidence ID and supplies the complete sequence to the solver. It performs no retrieval, state arbitration, consent reasoning, conflict resolution, or THSS projection; those governance capabilities are explicitly `UNSUPPORTED_BY_METHOD`.
