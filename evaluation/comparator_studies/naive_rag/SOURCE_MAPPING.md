# Naive-RAG source mapping

The method is a repository-defined baseline, version `exact-code-top5-v1`, implemented by `commitloop_baselines.naive_rag`. The exact-code rule avoids an unpinned embedding or reranking dependency.
