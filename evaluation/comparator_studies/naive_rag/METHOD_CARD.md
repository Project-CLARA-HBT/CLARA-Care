# Naive-RAG method card

This frozen local baseline selects at most five visible events by exact target system/code match while preserving source order. It has no embeddings, reranker, temporal arbitration, or governance; unsupported governance is returned explicitly.
