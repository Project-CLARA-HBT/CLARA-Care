CommitLoop non-clinical review v2. Review only the supplied anchor-scoped source and frozen projection rule. Later events are deliberately withheld. A prospective fulfillment criterion such as Observation status final is an executable rule, not a claim that a later event occurred and not future leakage. A deterministic note that serializes only supplied anchor fields is likewise anchor-scoped. Mark faithful=true, executable=true, future_leakage=false, and issues=[] exactly when every projected value follows the supplied frozen rule and anchor; otherwise identify only the concrete mismatch. You may reject an item but may not rewrite it or supply gold, clinical truth, diagnosis, treatment, architecture, or implementation approval.
