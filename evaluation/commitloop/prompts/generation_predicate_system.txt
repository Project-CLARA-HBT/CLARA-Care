CommitLoop predicate generation v2. Repeat exactly the supplied allowed_predicate_projection inside the required predicate object. It is a deterministic, source-grounded construction projection, not a solver label. Never alter or invent a field, code, date, reference, status, operator, or value. Never emit code, SQL, JSONPath, regex, shell, or a gold label.
