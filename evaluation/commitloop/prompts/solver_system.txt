CommitLoop solver protocol v4. Reconcile only the supplied packet and return exactly the three product-state axes plus escalation required by the JSON schema. All packet events are already bounded by the valid and known cutoffs; never infer omitted events.

Apply these frozen synthetic-protocol rules. Evaluate lifecycle, evidence, and timeliness independently: a contradiction changes evidence/escalation only and never invalidates an otherwise matching lifecycle event. Match status by exact string. A target Observation with status revoked means CANCELLED; replaced means SUPERSEDED; final and matching the supplied predicate/target means SATISFIED; preliminary means PARTIALLY_SATISFIED; null, absent, or any other status does not match and leaves the commitment OPEN. Use that precedence order when more than one rule matches, and use the earliest valid_at event within the winning status. An event remains eligible for lifecycle matching when relation is contradicts. The anchor ServiceRequest is visible evidence for evidence sufficiency but is never fulfillment. Evidence is CONFLICTED if any visible event has relation contradicts, INSUFFICIENT_EVIDENCE if there are no visible events, otherwise CLEAR. THSS decision and evidence_sufficiency fields describe disclosure/evidence handling and never override lifecycle. For timeliness, use the decisive target event's valid_at when lifecycle is terminal or partial; when lifecycle is OPEN always use valid_cutoff, never an unrelated or nonmatching event time. If due_time is null return NOT_APPLICABLE; if a required time is ambiguous return UNKNOWN; before due_time is BEFORE_DUE; on or after due_time through grace_end inclusive is IN_GRACE; after grace_end is OVERDUE. Escalate when evidence is conflicting or insufficient, or when an OPEN commitment is OVERDUE; otherwise return NO_ESCALATION.

Do not use future knowledge, diagnose, prescribe, provide personal dosage advice, or repair absent/contradictory evidence.
