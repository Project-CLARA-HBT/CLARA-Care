# Decisions and progress log

## Decisions

| Date | Decision | Reason / alternatives |
|---|---|---|
| 2026-08-06 | Preserve every capability through workspace More/context/direct links. | User previously rejected simplification that made the navbar look empty; hiding routes is not acceptable. |
| 2026-08-06 | Separate route access from navigation presentation. | Current coupling can turn a menu cleanup into an authorization regression. |
| 2026-08-06 | Do not invent patient directory or Admin Settings. | Source/backend does not provide those capabilities; labels must not imply features. |
| 2026-08-06 | Keep medication course, cabinet, and PHR models distinct. | Names alone cannot prove identity/provenance. |
| 2026-08-06 | Use typed bundled SVG icons with fallback. | Rounded Material Symbols is unloaded and external fonts are a resilience/privacy risk. |
| 2026-08-06 | Keep Chat rollback and legacy CSS until parity evidence. | Compatibility is a release safety requirement. |
| 2026-08-06 | Keep warnings/escalation visible; collapse only technical detail. | Progressive disclosure cannot hide medical safety information. |
| 2026-08-06 | No DB migration for core presentation milestones. | Existing contracts can support honest UI states; optional schema improvements need separate rollback. |
| 2026-08-06 | Treat `/dashboard` as a professional, role-aware workspace rather than a personal LifeMap home. | Route policy allows researcher/doctor/admin only; personal users already land on `/today`. The supplied mock informs hierarchy and visual language, not authorization or fabricated personal data. |
| 2026-08-06 | Preserve structured dashboard alerts through the web normalizer. | Flattening alert objects lost severity and destination, which could hide a critical warning or send the user to the wrong workflow. |
| 2026-08-06 | Never infer health stability from an empty operational alert list. | The dashboard now says only whether CLARA flagged work in the loaded scope and displays an explicit unknown state while loading or after failure. |
| 2026-08-06 | Use the supplied Clara Health System palette as the exact dark-mode token source. | It replaces the old charcoal override globally while preserving approved light-mode behavior and contrast guards. |
| 2026-08-06 | Represent blocked evidence release as insufficient evidence, not a partial clinical conclusion. | The UI offers only evidence refinement already present in Chat and the existing visit-preparation route; it does not imply clinician availability. |
| 2026-08-07 | Use the supplied Clara Health System dark design as the application default. | The reference is explicitly dark and applies to all web surfaces. Existing theme controls remain for preference compatibility, but the no-preference initial state is dark. |
| 2026-08-07 | Do not show a workspace selector in the personal shell. | Today is the default personal home, not a workspace destination. Clinical/research/admin selectors remain because they preserve authorized navigation. |
| 2026-08-08 | Do not render unsupported observability metrics as though they were measured. | Fixed node-count and estimated p95 labels were removed. Charts may visualize returned telemetry or clearly labelled client-side derivations, but never a fabricated operational value. |
| 2026-08-08 | Use Vietnamese as the safe default for mobile redesign copy when no language controller is available. | A silent English fallback would make safety and medication actions inconsistent with the Vietnamese product surface; an injected controller still takes precedence. |

## Audit feedback resolution

- Architecture audit: Critical workspace/access coupling and AppShell responsibility accepted as M2 priorities.
- Product/IA audit: Personal/Clinical/Research/Admin matrix accepted; direct links/More preserve access; “Bệnh nhân” rejected without backend capability.
- Design/a11y audit: raw icon bug, SideSheet focus, token cleanup, touch targets, live regions accepted as M1.
- Chat/Research audit: two-column answer-first disclosure and one canonical source list accepted as M4.
- Personal flows audit: honest dashboard, focused LifeMap/Visit/Family/PHR/Medicines flows accepted as M3; model merges rejected.
- Clinical/Scribe audit: canonical stage and finalized-vs-signed distinction accepted as M5; server contract remains authoritative.
- Testing/performance audit: typed fixtures, four viewports, axe, bundle budget, console/hydration checks accepted as M7.
- Planning review resolutions: Clinical primary routes use only existing `/dashboard`, `/chat`, `/council`, and `/scribe`; `/phr` is contextual and profile-kind labelled. The exhaustive route manifest is a pre-M2 gate. LifeMap/Visit/PHR contracts are conditional and must not be presented as saved when unsupported. More is labelled and ≤2 interactions. UI-bypass E2E is separated from real auth/RBAC/consent contract tests. Emergency/FIDES/DrugBank/no-CoT/Scribe and public-share shell boundaries are explicit test gates. Initial rollback is milestone revert; no shell flag is claimed before implementation.

## Progress

### Baseline (2026-08-06)

- Branch created: `feat/ui-modernization-v2`.
- Pre-existing user-owned changes preserved and not staged: `scripts/deploy/redeploy_app_stack.sh`, `CLARA_CODEX_MASTER_IMPLEMENTATION_SPEC.md`, `chathistory.md`, two demo JSON files.
- `npm run lint`: pass with eight pre-existing React Hook warnings.
- `npx tsc --noEmit`: pass.
- Vitest: 87 files / 677 tests pass.
- `npm run build`: pass; 91 routes generated; 104 kB shared first-load JS.
- Synthetic screenshots captured in `evidence/baseline/` for public landing/login and personal/clinical representative routes at desktop/mobile.
- Task screenshots were not available in the environment; this is documented in the current-state audit.

### Current gate

- Specification documents 00–11: created.
- Planning reviews: complete; three independent reviews were received and their resolutions are recorded above.
- `PLANNING_GATE`: passed on 2026-08-06 after route-matrix and bundle checks plus desktop/mobile accessibility and visual smoke passed against the standalone production artifact.
- Baseline E2E: `npm run test:e2e` on 2026-08-06, 6 passed / 2 failed / 2 skipped in 2m3s; failures are stale shell assertions and remain tracked for repair.

### Implementation checkpoints (2026-08-06)

- M1 foundation: typed SVG Icon/fallback and PHR icon migration. Targeted Vitest 5/5 and type-check pass. No API/schema changes.
- M2 shell: route access split from presentation, four workspaces, ≤7 primary items, More/context routes, profile/logout consolidation, mobile focus trap/restore. Navigation/unit tests pass; route matrix remains 79/79.
- M3 personal: Today no longer fabricates fallback cards/metrics; Family uses URL-backed Shared/Received/Access Log tabs; Visit preparation uses URL-backed four-step disclosure; Dashboard unknown states replace fabricated activity/council/confidence values. Personal presentation regression tests pass.
- M4 Chat: answer renders before collapsed explainability details; researcher/admin integrity diagnostics are collapsed and normal users do not receive them; browser-side hidden-reasoning/raw-confidence line filter added. Chat canvas tests 16/16 pass.
- M5 clinical: Scribe uses shared calm surfaces and explicit Record → Transcript review → SOAP review → Complete stepper; Council receives catalog-backed page heading. Existing Scribe safety/integration tests pass.
- M7 harness: `route-matrix:check`, `bundle:check`, i18n, type-check, desktop/mobile axe and visual smoke pass against standalone artifact. Full unit suite: 91 files / 694 tests passed after the post-review consent and answer-boundary coverage. Full E2E: 27 passed, 5 failed (shell assertions/temporary mobile proxy error under repair), 4 skipped.
- Post-review safety repairs: Scribe now requires an explicit consent checkbox and consent API capture before `getUserMedia`; completion is labeled as a draft rather than a signed/exported note. The answer boundary now filters common English/Vietnamese/XML reasoning markers. Mobile drawer initial focus excludes the backdrop and route navigation restores focus to its trigger. Dashboard no longer maps global runtime request totals to personal case counts.
- Dashboard visual follow-up: rebuilt from the provided reference using existing semantic tokens and typed SVG icons; kept the real professional route/RBAC contract; introduced researcher/doctor/admin shortcut sets; retained structured alert severity/message/href; removed medical reassurance from unknown/empty operational states; used `--text-brand` for dark-mode AA contrast; and added dashboard E2E at all four supported viewports. Independent review findings were repaired before deployment.
- Dashboard release: checkpoint `316a9599` pushed and deployed to `theclaracare.com`. VPS backup `/opt/clara-backups/pre-dashboard-20260806-135130.tgz`; fresh Node 20 image built successfully; unauthenticated `/dashboard` follows the intended login redirect; live mobile browser smoke has no page/5xx errors and security headers remain present.
- Today real-data follow-up: `/lifemap/today` now returns a read-only, timezone-aware seven-day completion projection plus the scoped task's episode title/version. `/today` selects active, completed, caught-up, or first-time presentation from those values only. It never auto-completes a task or turns a pending draft into a conclusion. `completeLifeMapTask` sends `If-Match` when the server supplied a version. After E2E found a real 22–23px tablet/mobile overflow caused by raw icon-font fallback text, the mobile notification and More icons moved to the bundled SVG abstraction; four-viewport Today E2E is now 12/12. Checkpoint `082cd3cc` was pushed and deployed on 2026-08-06: VPS backup `/opt/clara-backups/pre-today-20260806-143635.tgz`, API readiness is `ready`, and unauthenticated `/today` correctly redirects to login with security headers intact. No migration was required.
- Chat visual-system release: checkpoint `5faf71f0` applies Be Vietnam Pro and the supplied Clara Health System dark tokens, keeps Research inside Chat instead of duplicating a route, and renders an actual evidence-release failure as an insufficient-evidence boundary. Targeted Chat/token tests are 64/64, i18n/type-check pass, and the production web build passes with the pre-existing unrelated Hook warnings. It was deployed on 2026-08-06 after VPS backup `/opt/clara-backups/pre-chat-palette-20260806-150800.tgz`; `api` readiness is `ready` and public `/chat` redirects to login with CSP, HSTS, nosniff, frame denial, referrer and permissions policies present. No migration was required.
- PHR detail follow-up: added the persisted contact/insurance and allergy-state fields, paired body-measurement history/BMI endpoint, and data-derived detailed PHR screens. The responsive overview uses the supplied mobile four-card hierarchy while desktop retains all six direct section routes. A no-known-allergy declaration writes immediately; inactive medicines remain retained and can be marked current again. The focused PHR suite is 5/5, TypeScript and i18n checks pass, and lint has only the pre-existing unrelated Hook warnings. Web deployment of the latest medicine/mobile polish remains pending the interrupted production hand-off.
- Design-system shell checkpoint: created the full design-to-route map; changed the shared shell to the supplied `#101419` deep-well palette, 256px sidebar, 1120px canvas, 16px/48px gutters and low-contrast tonal card treatment. The personal sidebar no longer presents a workspace switcher. Council/Scribe local panels now reuse the same shared tokens. `npm run lint` passes with only seven pre-existing Hook warnings. No request, API, RBAC, consent, CSRF, telemetry or safety logic changed.
- PHR/Council screen checkpoint: converted the live PHR share/export/emergency/reminder/OCR surfaces and Council SVG flow canvas from local white/slate/sky gradients to the supplied individual screen treatments. Focused PHR/Council tests pass 23/23; `npm run lint` still has only the seven pre-existing Hook warnings. These components retain their existing consent, revocation, manual-review, emergency and role behavior. Rollback is a component-only revert.
- Medicines consent screen checkpoint: aligned the real consent-required gate, legal links, disabled action and retry/error boundary with the supplied dark palette. The acceptance mutation and its CSRF/consent gating are unchanged. Focused medicines tests pass 7/7; rollback is component-only.
- Research workspace screen checkpoint: aligned live frame/search/synthesis/watch states, including the clarification boundary and result rail, with the supplied tonal card system. Source selection/upload/job execution and role routing are unchanged. Focused Research tests pass 31/31; lint only reports the seven tracked pre-existing Hook warnings. Rollback is component-only.
- CSRF profile-save repair: browser production smoke verified login → `/phr/identity` → save and the API's expected missing-header 403 / matching-header 200 boundary. The web cookie reader now selects the final duplicate-name CSRF cookie, matching the server parser after a cookie-domain migration; a regression test protects this stale-session case. No CSRF exemption or backend policy was added.
- Council empty-state E2E repair: an owner with no Council case previously caused the client to request `/council/cases/latest`, receive its expected 404 and render that as a loading error. The three Council entry views now request one owner-scoped list item and render a normal empty state when it is absent; stale local case selection is cleared. Focused Council tests pass 10/10 and type-check passes.
- LifeMap/operational visualization checkpoint: `/lifemap` now puts profile-scoped journeys and accepted Today tasks before optional expert tools. The empty state leads only to the existing guided journey workflow and hides the task form until an episode exists. Dashboard/Admin card frames use the supplied deep-well card language; fixed ML-cluster and p95 estimates were removed from observability. Targeted LifeMap/Admin tests pass 7/7; lint and TypeScript checks pass. This is UI-only and leaves LifeMap, telemetry, consent and RBAC contracts unchanged.
- Council/Scribe workflow-surface follow-up: case creation/result primary actions are solid token-based controls and warning/escalation panels now use semantic safety tokens. Scribe workspace tabs, active sessions and its first-use state no longer use white/slate/shadow treatments. The recording-consent and draft/finalization behavior is unchanged. Scribe tests pass 7/7 with Council/Scribe lint and TypeScript checks.
- Ecosystem operational surface follow-up: the dashboard ecosystem header, actions, snapshot metadata and semantic statuses now use the canonical deep-well palette. Its counts and visual bars remain direct/derived views of `getSystemEcosystem`; unavailable server values remain visibly unavailable rather than being replaced with sample telemetry. Ecosystem lint and TypeScript checks pass.
- Dashboard responsive evidence refresh: the production-artifact Playwright matrix passed 8/8 across desktop, laptop, tablet and 390px mobile. It checks structured alert/next-action fidelity, non-reassuring failed state and no horizontal overflow; four synthetic no-PII screenshots were produced under `apps/web/test-results/`.
- Admin visual follow-up: Runtime Monitor, source controls and the live retrieval-flow SVG now use the shared deep-well surfaces, semantic action/status states and the exact Clara palette. The SVG continues to derive node state from live RAG-flow configuration; its former decorative edge gradients and the user-facing “Fallback Branch” label were removed. Targeted lint and TypeScript checks pass. This is presentation-only and does not change policy, retrieval, or runtime configuration semantics.
- Fail-closed generation checkpoint: generation and research no longer retry a safe-mode/local-synthesis path after a model, retrieval, or ML upstream failure. The legacy Control Tower field is retained only to parse historical records and is normalized to `false`; the ML service independently hard-disables it. The API returns an explicit retryable 5xx boundary instead of a substituted answer, while legal cross-border consent gating remains a separate non-LLM policy response. Python modules compile and targeted web lint/type checks pass; the local environment has no `pytest` executable, so focused API tests were updated but not executed here.
- Route visual sweep follow-up: the remaining Source Hub, Council, Scribe and Ecosystem result/table/empty states now share the exact deep-well tokens. Admin Answer Flow, Runtime Monitor and live SVG Flow Visualizer use tonal data surfaces, preserve their live API-derived inputs and represent low-context as a fail-closed policy boundary. Targeted lint and TypeScript checks pass. Control Tower and the detailed observability/overview panes remain the final active visual sweep.
- Route verification and token checkpoint: Control Tower, observability/overview, Flow Debugger and the Flow Visualizer are now part of the same deep-well sweep; its SVG remains driven by live flow flags and has no decorative gradient or fallback branch. Council, Scribe and Source Hub no longer bypass the shared palette with arbitrary hex utilities; `--card-top-border` preserves the supplied `#2A3950` structural edge through a token. Full Vitest passed after this repair; the LifeMap production-artifact Playwright matrix passed across desktop, laptop, tablet and 390px mobile. Commit `4b6bc4c9` was pushed; web deployment was started and service health remained available throughout the image rebuild.
- Clinical/research state verification checkpoint: Source Hub no longer uses Tailwind `blue-*` utilities for its summary, source chips or input focus state; those controls now consume the canonical brand/surface/text tokens. A new production-artifact Playwright spec uses only a public-source metadata fixture and empty owner-scoped workspaces to verify Evidence, Source Hub result, Council first-use and Scribe first-use states at desktop, laptop, tablet and 390px mobile. It confirms no visible application/traceback error, but does not claim clinical validation. Lint, TypeScript, 12-token Vitest checks and the four-viewport E2E matrix passed. Commit `0a9cddbe` was pushed and the web/API containers were rebuilt on the VPS; ML internal health was `ok` and the four protected routes retained their intended login redirects after deployment.
- Evidence result disclosure checkpoint: the same four-viewport production-artifact E2E suite now drives the explicit Evidence workflow (save question → user confirmation → verified run) and checks the rendered source matrix, applicability boundary, uncertainty disclosure and document reflow. The fixture contains public-source interface metadata only and explicitly states it is not a clinical conclusion. This adds result-state evidence without bypassing consent, source release or user-confirmation behavior. Lint, TypeScript, token checks and the expanded matrix passed; commit `bedcc5d1` contains tests only, so no application deployment was required.
- Council clinical-flow token checkpoint: Council new/intake/specialist/review/result screens, Evidence matrix cards and the shared `SurfaceCard` now consume `--card-top-border`, `--on-secondary-container` and semantic danger tokens rather than local palette literals. Their existing user-review, result handling and navigation stay unchanged. Focused lint, TypeScript and 17 token checks passed. Commit `47228805` was pushed and deployed; the rebuilt web/API containers were healthy, ML internal health was `ok`, and `/evidence`, `/council`, `/council/new`, `/council/result` retained the intended unauthenticated login redirects.
- Scribe active-state verification checkpoint: the production-artifact clinical/research E2E suite now includes an owner-scoped, non-PII Scribe draft fixture alongside first-use states and the explicit Evidence result flow. It verifies the transcript/SOAP review hierarchy, keeps recording-data deletion absent when the capability endpoint is unavailable (fail closed), and checks document reflow. All 12 cases passed across desktop, laptop, tablet and 390px mobile. The fixture is interface-only and is not presented as clinical validation; no recording, consent, RBAC, completion, or server-authority behaviour was changed. The forthcoming test-only commit requires no application deployment.
- Council result-state verification checkpoint: the same production-artifact suite now renders an owner-scoped Council result fixture that contains no patient data and states explicitly that it is not a clinical conclusion. It verifies the results hierarchy, human-review disclosure, unavailable DrugBank safety boundary and reflow at all four supported viewport classes. All 16 cases passed. This is interface regression evidence only; it does not constitute a Council model evaluation, clinical review, medication-safety validation or a bypass of server-side role and ownership checks. The forthcoming test-only commit requires no application deployment.
- Mobile terminology and OCR checkpoint: Cabinet OCR now obtains copy from the active language controller and defaults safely to Vietnamese when that controller is absent. The unified mobile surface syntax regressions were repaired without changing data or safety behavior. Focused Flutter redesign/home/a11y tests passed 8/8 and Cabinet OCR tests passed 4/4. `flutter analyze` reported no errors; four pre-existing informational deprecations/doc notices remain. UI-19 remains open because browser CSRF, four-viewport visual, and complete accessibility evidence are separate release gates.
- Field primitive accessibility checkpoint: shared input, textarea and select controls now enforce the documented 44px touch target and expose hint/error text through `aria-describedby`, with `aria-invalid` and an assertive error announcement. This applies to PHR, medicine, visit, family and onboarding forms without changing any request, CSRF, consent or validation policy. Focused Field tests pass 2/2; web type-check passes; lint has six existing Hook-dependency warnings outside this primitive.
- Visualization palette checkpoint: Dashboard/Admin ring gauges and telemetry bars now use the documented primary/secondary/tertiary/error palette and tonal tracks. Decorative gradients, neon glow, slate/white tracks and out-of-system green/purple/rose values were removed. Metric calculations and the availability boundary are unchanged. Admin observability tests pass 3/3; type-check and lint pass with the same six existing Hook warnings.
- Shared shell/auth resilience checkpoint: all shared navigation renderers and the mobile command/drawer controls now resolve navigation meanings to bundled SVGs, so an unavailable Material Symbols font cannot expose raw glyph names or question-mark icons. The mobile drawer uses tonal depth rather than blur/shadow, and the authentication shell, inputs and feedback use the supplied deep-well palette, 14px card framing and semantic status tokens. No authentication, RBAC, cookie, CSRF, consent or logout behaviour changed. Focused app-shell, auth-field, Field and Surface tests pass 9/9; type-check passes; lint retains only the six unrelated Hook-dependency warnings.
- Scribe/clinical palette checkpoint: the enterprise Scribe transcript, consent, process, grounding, coding, amendment and sign/export presentation has been converted from a light blue/green/red theme to the canonical tonal cards and primary/secondary/tertiary/error status tokens. Chat overlays and the desktop top bar now use the same quiet deep-well scrim, without blur/shadow decoration, and all top-level navigation controls use bundled SVG icons. Existing consent capture, recording, stream/batch transport selection, clinician confirmation and signed-note authority are unchanged. Scribe safety/workflow tests pass 27/27, AppShell tests pass 4/4 and web type-check passes.
- Dialog accessibility checkpoint: the shared modal now has a visible-title `aria-labelledby` relationship, optional description `aria-describedby`, the supplied tonal top edge and a quiet opaque deep-well scrim. Escape, focus trapping and focus restoration remain intact. Focused Modal, Field and Surface tests pass 6/6 and type-check passes. UI-04 remains in progress until the remaining dedicated side-sheet/confirmation entry points have equivalent evidence.
- Knowledge Sources visual checkpoint: the live Admin Knowledge Sources route now uses tonal Clara cards, low-contrast structure, semantic success/error states and brand focus treatment throughout connector controls, source allocation, document management and federated-source tables. It remains an admin-only route and its loading/degraded/error paths, mutations and source synchronization contracts are untouched. Type-check passes; lint retains only the existing Hook-dependency warnings (including two already present in this screen).
- Chat/Evidence/PHR icon-resilience checkpoint: the active Chat V2, Evidence and PHR screens no longer render Material Symbols glyph names or the generic SVG fallback. Chat command/workspace controls now have compile-time supported semantic mappings, with a regression test that asserts both resolve to concrete bundled SVGs. The chat welcome, sheet and command-palette treatments use tonal depth rather than hero shadows; Evidence and PHR retain their existing source-release, consent, polling, export and record-mutation contracts. Chat/Canvas parity tests pass 62/62, focused PHR tests pass 6/6, and web type-check passes.
- Public landing checkpoint: the public landing page now follows the same canonical deep-well Clara token system as the authenticated product instead of the previous cyber-grid/neon/slate treatment. All landing sections, i18n copy, sponsor links and product/legal CTAs remain available; raw Material and Font Awesome icon dependencies were replaced with bundled semantic SVGs. The shared icon and button paths now resolve an unexpected runtime glyph to a meaningful `clinical-notes` SVG rather than rendering a question-mark fallback. Focused landing/icon tests pass 6/6, TypeScript passes, and lint has only the six existing Hook-dependency warnings outside this change.
- Medicines per-screen checkpoint: Cabinet, tracked-medicines, consent-accepted and guided add/scan states now use bundled SVGs and the canonical semantic status tokens instead of Material glyphs, emerald and white primary-button text. Cabinet loading/mutation, DrugBank display, OCR confidence, consent acceptance and interaction eligibility remain unchanged. Focused Medicines tests pass 10/10 and TypeScript passes.
- Shared primitives and longitudinal-workflow checkpoint: `Badge` and accessible `Tabs` now translate any persisted Material identifier through the bundled semantic SVG registry, covering Family, Visit and Medicines tabs without risking raw icon-name or question-mark output. Council, LifeMap, guided-flow validation/save, task detail, guidance, role redirect and legacy Chat composer controls now use the same structural deep-well tokens. The active Markdown answer renderer—including generated chart, Mermaid, citation registry, tables, code and export states—uses the supplied surface/status palette rather than light slate/cyan surfaces; its safety, sanitisation, citation and export behaviour are unchanged. Targeted icon/Council/LifeMap tests pass 14/14; TypeScript passes; lint retains the six pre-existing Hook warnings outside this scope. The remaining legacy Chat page is intentionally still a separately tracked visual sweep because it contains a large inactive compatibility workspace.
- Legacy Chat icon-resilience checkpoint: the feature-flagged rollback Chat workspace now also routes every visual icon through the same bundled semantic SVG registry, rather than relying on the Material Symbols font. This preserves the rollback flag and all chat/research/telemetry data paths while ensuring neither default nor compatibility Chat can show raw glyph names or a `?` substitution when the external font is unavailable. `rg` finds no production `material-symbols-outlined` use in `apps/web`; focused legacy-safety/icon tests pass 5/5 and TypeScript passes. Full visual convergence of the compatibility layout remains a separate route-level task.
- Shared status-control checkpoint: consent toggles, legal status/CTA surfaces, the transparency gate, community dialog action, default danger button and Chat V2 primary actions now use the explicit supplied foreground/status tokens instead of browser-white/cyan/emerald utilities. Consent mutation, legal documents, community actions and disclosure gating remain unchanged. Focused transparency/surface tests pass 8/8 and TypeScript passes.
- Admin action parity checkpoint: knowledge-source management, RAG ingestion and RAG evaluation primary actions now use the canonical `on-secondary-container` foreground and compact 8px control geometry. Source synchronisation, ingestion, evaluation and existing admin route authorization do not change. The focused RAG evaluation suite passes 15/15 and TypeScript passes.
- PHR dialog accessibility checkpoint: the PHR modal adapter now delegates to the shared labelled modal rather than maintaining a separate Escape-only implementation. It inherits focus trapping/restoration, scroll lock, backdrop close and the dark mobile-sheet/desktop-card treatment; its capability flags, OCR/manual-review and data mutation flows are unchanged. The shared modal accepts a localized close label. Focused Modal/PHR tests pass 8/8 and TypeScript passes.
- Community dialog parity checkpoint: the flag-gated Community post detail now uses the shared modal as well, retaining the server-moderated comment, reaction and report flows while inheriting labelled modal/focus/scroll semantics and the canonical tonal surface. Unsafe-content moderation remains fail-closed; no PHR/medical data is introduced. TypeScript and focused Modal tests pass 2/2.
- Council action-dialog checkpoint: specialist handoff and privileged guard override/pause dialogs now use the shared focus-trapped modal with localized labels, safe Escape/backdrop close and responsive sheet/card treatment. The role checks, reason requirement and actual guarded action handlers remain unchanged. Council safety/stream plus Modal tests pass 10/10 and TypeScript passes.
- Scribe destructive-dialog checkpoint: recording-data deletion now uses the same focus-trapped shared modal as the rest of the product, with `alertdialog` semantics, a visible localized description and responsive deep-well sheet/card treatment. The recording-data capability check, delete authorization and deletion request are unchanged. Modal/Scribe focused tests pass 11/11; TypeScript and lint pass, with only the six previously tracked Hook-dependency warnings outside this change.
- Active shared-surface follow-up: the legacy Chat user bubble, PHR emergency-card disclaimer, application top-bar notification count, dormant role sidebar and Admin conduit link now consume the canonical deep-well/status palette, with no light cyan/slate/default-white bypass. Existing PHR data access, profile selection, legacy-chat rollback and observability calculations remain unchanged. TypeScript and lint pass with the same six tracked Hook-dependency warnings.
- Transparency-gate accessibility checkpoint: the acknowledgement-only notice now locks document scrolling, sends initial focus to its sole acknowledgement action, and traps `Tab` until the required version is recorded. It remains intentionally non-dismissible and fail-open only when the notice cannot be read; no consent, emergency, or acknowledgement API behavior changed. The panel also uses the canonical 14px tonal frame rather than a heavy shadow. Transparency and Modal tests pass 10/10; TypeScript passes.
- Research compatibility-panel checkpoint: retained empty/latest/history Research renderers now use the same canonical cards and semantic warning/reference/restricted states as the active Chat workspace. Existing role-gated diagnostic labels and historical fallback field rendering were not changed; production generation remains fail-closed. TypeScript and lint pass with the six tracked unrelated Hook warnings.
- Research workspace compositor checkpoint: the retained composer/result/verification/evidence-step presentation now matches the same deep-well 14px card, 8px control and semantic status language as the supplied Research references, with decorative light gradients and shadows removed. Query/upload, tier/mode/retrieval selection, citation links and admin-only diagnostics remain unchanged. TypeScript and lint pass with the six tracked unrelated Hook warnings.
- Legacy Research rail follow-up: all user-facing compatibility rail cards, evidence agreement/source selection/upload controls and responsive tab switcher now consume the canonical tonal palette and semantic success/warning/error states. Detailed Flow/Telemetry diagnostic panes remain separately role-gated and are retained for compatibility; their current visual audit remains part of UI-20 rather than a claim that obsolete runtime telemetry is an End_User feature. TypeScript and lint pass with the six tracked unrelated Hook warnings.
- Cross-route visual regression checkpoint: the full Playwright suite completed successfully after the shared/dialog/Research changes (`72` tests across `6` specs, including desktop and mobile Chromium matrices). It covers core shell states, Today truthful states, dashboard and clinical/research fixtures plus public visual smoke. This validates application reflow and regression fixtures; it is not clinical validation or evidence that every possible persisted-data combination has visual review.
- Legacy diagnostic status checkpoint: Research Flow Timeline and telemetry evidence-support mappings now derive their marker, connector and badge appearance only from the canonical neutral/brand/warning/danger tokens. Stage/event, runtime and role-gating logic is unchanged. TypeScript passes.
- VPS deployment checkpoint (2026-08-09): source revision `e4bd552b4778ca4f1e1f4a7b0a80d6be865aad9e` was archived to `/opt/clara-care` and deployed to the controlled VPS. A recoverable pre-release source backup exists at `/opt/clara-backups/pre-ui-20260809-173953.tgz`; Docker volumes and the operator-owned `.env` were retained. The deployment guard was corrected to validate an explicitly configured OpenAI-compatible HTTPS `/v1` endpoint and safe, distinct governed model IDs rather than pinning retired `deepseek-v4-*` identifiers. It continues to reject a fallback model, disabled registry/routing, missing credentials, invalid timeout ordering and invalid strict DrugBank configuration. `api`, `ml`, `web` and `asr` were rebuilt/recreated; Alembic reports `20260808_0050 (head)`. Host health endpoints report API/ML `ok`, ASR `ready`, DrugBank integrity `verified`; public `/chat` gives the intended login redirect with CSP, HSTS, nosniff and frame-denial headers. No user data, consent/RBAC behaviour, or database migration was introduced by the UI release.
- LifeMap active-state checkpoint: the primary route now gives real journey records and the tasks the user has accepted into Today their intended reference order: a structured journey list first, followed immediately by the accepted-task list. Each accepted task may show only its actual owning episode title; no progress percentage, due time, completion state or recency is invented. The duplicate lower-page task list was removed. Semantic bundled SVGs replace the unsupported route/checklist identifiers in this screen. TypeScript plus 19 focused LifeMap tests pass; the browser accessibility/reflow matrix passes 4/4 across desktop, laptop, tablet and mobile, including the active-state hierarchy.
- Legacy diagnostic-panel completion: retained Research Flow Timeline, role-gated telemetry detail and Markdown export menu no longer depend on slate/cyan/white utilities or a heavy menu shadow. Their event/status/raw-diagnostic structures are unchanged, but the visual states now use the canonical deep-well, brand, warning and danger tokens consistently. TypeScript passes and the production TSX palette scan is clean for these three modules.
- Legacy Chat visual/accessibility convergence checkpoint: the feature-flagged compatibility workspace now uses the same deep-well surface stack, semantic brand/warning/danger states and quiet popovers as the active Chat screen. Its folder manager and command palette delegate to the shared labelled, focus-trapped modal, restoring scroll lock and focus behavior rather than retaining bespoke overlays. Decorative blue/cyan/amber/rose utilities, light surfaces, gradients, backdrop blur and heavy shadows were removed from the user-facing compatibility layout. Conversation, export, share, note, telemetry, folder and command-palette interactions are unchanged; the workspace remains a rollback-compatible presentation only. TypeScript and focused legacy safety tests pass; the targeted legacy palette scan is clean.
- Shared dialog/accessibility completion: the dialog inventory now resolves to either the shared focus-trapped modal or a purpose-built drawer/palette with equivalent `aria-modal`, focus trapping, Escape/backdrop close and focus restoration. The Field/Shell/Modal plus legacy regression suite passes 14/14; UI-04 is complete. This verifies interaction primitives, not every route's content-specific keyboard workflow; cross-route responsive/accessibility coverage remains UI-19.
- Canonical CSS depth checkpoint: the dark-theme shadow aliases are now explicitly `none`, so older components that correctly consume `--shadow-*` cannot reintroduce drop-shadow depth. The final compatibility overrides also make legacy Chat/Chat V2, retained glass/chrome/research panels, diagnostic bars and research chips consume the supplied deep-well stack even when an older local selector wins by source order. No runtime data, route, safety or interaction logic changed; TypeScript and lint pass with the six tracked unrelated Hook warnings.

### Checkpoint template

```text
Checkpoint:
Commit:
Scope/files:
Requirements/tasks:
Tests run:
Pass/fail/not-run evidence:
Safety/privacy review:
Rollback target:
Open follow-up:
```

## Rejected or deferred items

- New patient directory and Admin Settings route: rejected as non-functional invention.
- PHR PATCH/ETag, recurring reminders, and server-resumable Visit fields: deferred optional API work; UI must fail honestly until available.
- Full mobile Flutter redesign: out of scope; terminology synchronization remains required.
- Bulk deletion of legacy CSS/Chat: deferred until import/flag/visual evidence.
