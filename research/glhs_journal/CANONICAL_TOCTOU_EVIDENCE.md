# Canonical GLHS PostgreSQL TOCTOU Evidence — v1 (historical) vs v2 (manuscript-cited)

Status: **byte-verified**. Claim-eligibility of the current v2 raw run: **UNRESOLVED** (see §4).
Generated by Workstream A (R3 audit) on 2026-08-19. This document is a pointer target for
`research/publication_registry.yaml` (GLHS canonical TOCTOU evidence) and does not overwrite,
modify, or reclassify any sealed artifact. All sealed artifact directories (`artifacts/**`,
`research/glhs_journal/protocol_v2/**`) remain byte-for-byte unchanged.

## 1. TL;DR

| Item | v1 | v2 |
| --- | --- | --- |
| Run ID | `GLHS-POSTGRES-TOCTOU-FINAL-20260817-01` | `GLHS-POSTGRES-TOCTOU-FINAL-V2-20260817-01` |
| Schedules | 5 (`TOCTOU-01..05`) | 12 (`TOCTOU-V2-01..12`) |
| Executed source revision | `2074f87550c5ee32302bde47bc0b9e6be6af36b5` | `67e528b61512aabc201b344e54f9e3e724490e41` |
| Artifact root | `artifacts/glhs-postgres-toctou/GLHS-POSTGRES-TOCTOU-FINAL-20260817-01/` | `research/glhs_journal/protocol_v2/` |
| Seal manifest | `artifact-sha256.json` (3 entries) | `artifact-sha256.json` (4 entries) |
| Hash verification | ALL PASS | ALL PASS (4/4) |
| Raw run status | `SEALED` (v1 analysis.json) | `EXECUTED_V2_OBSERVATION_MISMATCH` |
| Claim-eligible | Historical; superseded as manuscript headline | **NOT currently** (see §4) |
| Role | Historical evidence (retained immutable) | **Canonical manuscript-cited 12-schedule artifact** (byte-verified) |

## 2. v1 — historical (5-schedule)

- Run: `GLHS-POSTGRES-TOCTOU-FINAL-20260817-01`, 5 schedules, source `2074f875`.
- Raw root: `artifacts/glhs-postgres-toctou/GLHS-POSTGRES-TOCTOU-FINAL-20260817-01/`.
- Seal `artifact-sha256.json` entries recomputed and verified on 2026-08-19:

| File | Declared sha256 | Recomputed | Result |
| --- | --- | --- | --- |
| `result.json` | `0fa32514f41c408a65271fe7c93ab5892c9b82d357f5611ba89e468b3e586d54` | same | PASS |
| `analysis/analysis.json` | `86fc839df2cd7d2c732696e5173b2ce6a9e1e500ebddd88abae401b67cfc44fb` | same | PASS |
| `tables/schedule_matrix.csv` | `a7d79c307e96d3c3dcc87eadd521880566c369e4adb658d65567de97b5858970` | same | PASS |

v1 remains frozen and untouched (GR-02 historical immutability). It is retained as the historical
5-schedule predecessor. The manuscript-cited 12-schedule result is v2, not v1.

## 3. v2 — canonical manuscript-cited artifact (12-schedule), byte-verified

- Run: `GLHS-POSTGRES-TOCTOU-FINAL-V2-20260817-01`, 12 schedules (`TOCTOU-V2-01..12`).
- Executed source revision recorded in `run_v2_raw.json` as
  `67e528b61512aabc201b344e54f9e3e724490e41` (= git commit `67e528b6`, present: `git cat-file -t 67e528b6` → `commit`).
  That commit's diff is `evaluation/glhs_postgres_toctou/executor_v2.py` +
  `tests/test_executor_v2.py` (the v2 executor + its test), i.e. the protocol-JSON artifacts are
  sealed working-tree files and are not inside that commit's diff.
- Artifact root: `research/glhs_journal/protocol_v2/` (read-only seal boundary).

### 3.1 Seal `artifact-sha256.json` verification (recomputed 2026-08-19)

| File | Declared sha256 | Recomputed | Result |
| --- | --- | --- | --- |
| `postgres_toctou_protocol_v2.json` | `c10836f3e25c29cd83ade06cf104f67d97484397ec037809a36482685830798d` | same | **PASS** |
| `statistics_plan_v2.json` | `46a7e270f067769a3c43dc643dd448a2a25bdfffe74597c2d3b399b073af4fa7` | same | **PASS** |
| `run_v2_raw.json` | `ae8932f69b87e825b3ec942a453aa97b60156581c90a694f9ccdfc79b19c3d8d` | same | **PASS** |
| `analysis_v2.json` | `39efc4c7632e305eaea6b7ea12c339752266a901ad931fda806e58f207d95990` | same | **PASS** |

Not sealed (no entry in `artifact-sha256.json`): `postgres_toctou_protocol_v2_1.json`
(sha256 `0b36ec3af7d1e9c43c92d6d825922d2f7364772dfdd2decf42126e0d6fe425c4`, untracked in git).
`README.md` (sha256 `e7f431efb6948d846d65cfe3848bd3d71a9d5e7f5c8a87d174e7a4445a6a0a54`) is the run book,
not part of the byte seal.

### 3.2 Structural conformance of `run_v2_raw.json`

- `run_id`: `GLHS-POSTGRES-TOCTOU-FINAL-V2-20260817-01` — matches protocol `postgres_toctou_protocol_v2.json`. PASS.
- `schema_version`: `glhs-postgres-governance-toctou-final-v2` — matches protocol. PASS.
- Schedule count: 12 (`TOCTOU-V2-01..12`), matching the frozen 12-schedule protocol. PASS.
- Per-schedule observations: observer-complete with `rejection_auditability` /
  `committed_reconstructability` / `transaction_trace` / `latency_ms` as required. PASS.
- Classification audit: 10/12 `matches=true`; **2 mismatches** — `TOCTOU-V2-05` and `TOCTOU-V2-09`
  expected `indeterminate_ordering`, observed `rejected_*` (see §4).

### 3.3 Headline observed outcome (v2, 12 schedules)

Committed: 1 (`TOCTOU-V2-10` committed; `TOCTOU-V2-07` is the commit-before-revoke control).
Rejected: 10. Forbidden commits observed: 0. Indeterminate (observed): 0. Operational deadlock: 0.
Persisted-writer coverage: `advance_governance_policy_epoch`, `consent_revoke`, `role_change`.
These numbers appear in `analysis_v2.json` and are what the manuscript cites.

## 4. Honest status — UNRESOLVED claim-eligibility (no failing hash)

Every sha256 in both seals **PASSES**; no hash mismatch exists. However, verification of
*binding to a claim-eligible run* is only partial:

1. `run_v2_raw.json` top-level `status` is **`EXECUTED_V2_OBSERVATION_MISMATCH`**, and its
   `validation` block is `VALIDATED_V2_OBSERVATIONS_NOT_EXECUTED` with `database_executed=false`
   and `result_emitted=false`.
2. The frozen protocol's own `result_rule`
   (`postgres_toctou_protocol_v2.json`) states: *"An observed classification that differs from the
   frozen expected classification is recorded as an explicit mismatch and the run is not
   claim-eligible."* The `run_v2_raw.json` note repeats this.
3. Mismatched schedules: `TOCTOU-V2-05` (expected `indeterminate_ordering`, observed
   `rejected_after_or_during_governance_race`) and `TOCTOU-V2-09` (expected
   `indeterminate_ordering`, observed `rejected_during_or_before_governance_race`).
   Both observed outcomes are conservative (rejected rather than indeterminate), but per
   fail-closed rules a mismatch is a mismatch.

**Consequence for the manuscript:** the 12-schedule v2 artifact is the canonical *byte-verified*
manuscript-cited evidence, and it may be described as such with hashes. It may **not** be cited as
a claim-eligible / pass-status concurrency result in its current raw state. Until a re-run
produces `EXECUTED_V2_FROZEN_OBSERVATIONS` (or the two mismatches are reconciled against a frozen
revision and the run resealed under a new run ID), the claim-eligibility status stays
**UNRESOLVED** (tracked under R3 blocker `toctou_discrepancy`, Workstream D-001/D-004).

There is no failing hash to report; the unresolved item is the run-status/claim-eligibility
binding, not byte integrity.

## 5. No-overwriting guarantee

No file under `artifacts/**`, `research/glhs_journal/protocol_v2/**`, or any sealed registry was
modified to produce this document. v1 files, v2 protocol files, and both `artifact-sha256.json`
seals are byte-identical to their recorded state at R3 audit time.